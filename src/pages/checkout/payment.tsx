import { PaymentForm } from '../../components/payment/PaymentForm';

export const PaymentPage = () => {
    return (
        <div className="min-h-screen bg-bg-alt py-12 px-4 md:px-0 flex items-center justify-center">
            <PaymentForm />
        </div>
    );
};